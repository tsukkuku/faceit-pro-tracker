export interface PlayerStats {
  lifetime: {
    matches: string;
    avgKDRatio: string;
    avgHeadshots: string;
    longestWinStreak: string;
    winrate: string;
    recentResults: string[]
  };
}
